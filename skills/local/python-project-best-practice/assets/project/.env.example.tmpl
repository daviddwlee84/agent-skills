# Copy to .env and fill in. .env is gitignored; this file is the tracked
# contract of what the app reads. Keep every key here, with a safe default or
# an obvious placeholder.
#
# Every variable is prefixed ENV_PREFIX_PLACEHOLDER (see src/PACKAGE_NAME_PLACEHOLDER/settings.py).

ENV_PREFIX_PLACEHOLDERLOG_LEVEL=INFO
ENV_PREFIX_PLACEHOLDERLOG_JSON=false
